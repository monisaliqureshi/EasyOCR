# Install torch and torchvision BEFORE this package using the CPU-only wheel:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# (or use requirements-cpu.txt which does this automatically)
torch
torchvision>=0.5
opencv-python-headless
scipy
numpy
Pillow
scikit-image
python-bidi
PyYAML
Shapely
pyclipper
